services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    # ホットリロードを有効にする場合は以下のコマンドを使用 (開発時のみ)
    # command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload